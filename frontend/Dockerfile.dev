FROM node:20-alpine
WORKDIR /app

# Для быстрого старта в dev — ставим deps внутри контейнера
COPY package.json package-lock.json* ./
RUN npm install

# Код монтируется томом из compose, поэтому дальше ничего не копируем
EXPOSE 3000
CMD ["npm", "run", "dev"]
